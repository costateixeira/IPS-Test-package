# GENERATED by build-test-ig.mjs. Regenerate rather than edit.
id: hl7.fhir.uv.ips.test
canonical: http://example.com/fhir/hl7.fhir.uv.ips.test
url: http://example.com/fhir/hl7.fhir.uv.ips.test/ImplementationGuide/hl7.fhir.uv.ips.test
name: IPSTesting
title: "IPS: TestPlans and Gherkin for the IPS actors"
description: >-
  A demonstration IG that declares, as TestPlan resources and Gherkin feature files, the conformance tests for the three actors of the HL7 International Patient Summary (IPS) 2.0.1: Creator, Consumer and Server. The tests are derived mechanically from the obligations the IPS profiles state, and every obligation is traceable to a test case that names its element.
status: active
license: CC0-1.0
date: 2026-09-20
version: 0.1.0
fhirVersion: 6.0.0-ballot5
copyrightYear: 2026+
releaseLabel: ci-build
publisher:
  name: Jose Costa Teixeira
contact:
  - name: Jose Costa Teixeira
    telecom:
      - system: url
        value: https://github.com/costateixeira
jurisdiction: http://unstats.un.org/unsd/methods/m49/m49.htm#001 "World"

# TestPlan lives in the FHIR Testing IG (removed from R6 core at 6.0.0-ballot5).
dependencies:
  hl7.fhir.uv.testing: current
  # The specification whose actors these plans test — tracking only, so the
  # canonicals the plans point at resolve. A cross-version reference when the
  # spec is R4: only canonicals are used, no profiles are derived.
  hl7.fhir.uv.ips:
    id: ips
    uri: http://hl7.org/fhir/uv/ips/ImplementationGuide/hl7.fhir.uv.ips
    version: 2.0.1

resources:
  # TestPlan is an "additional resource": the publisher needs a root-level
  # resourceDefinition that FSH cannot emit; _package.py injects it after SUSHI.
  TestPlan/ips-creator-tests:
    name: IPS Creator Test Plan
    description: Declares the Gherkin test cases for the IPS Creator actor of HL7 International Patient Summary (IPS) 2.0.1.
    exampleBoolean: false
  TestPlan/ips-consumer-tests:
    name: IPS Consumer Test Plan
    description: Declares the Gherkin test cases for the IPS Consumer actor of HL7 International Patient Summary (IPS) 2.0.1.
    exampleBoolean: false
  TestPlan/ips-server-tests:
    name: IPS Server Test Plan
    description: Declares the Gherkin test cases for the IPS Server actor of HL7 International Patient Summary (IPS) 2.0.1.
    exampleBoolean: false
  # ===== RENDERING ONLY — delete this block, path-binary below, and input/fsh/binary-gherkin.fsh together
  Binary/ips-creator-gherkin-script:
    name: IPS Creator Gherkin Script
    description: Gherkin feature file with the test scenarios for the IPS Creator actor.
    exampleBoolean: false
    extension:
      - url: http://hl7.org/fhir/tools/StructureDefinition/implementationguide-resource-format
        valueCode: text/x-gherkin
  Binary/ips-consumer-gherkin-script:
    name: IPS Consumer Gherkin Script
    description: Gherkin feature file with the test scenarios for the IPS Consumer actor.
    exampleBoolean: false
    extension:
      - url: http://hl7.org/fhir/tools/StructureDefinition/implementationguide-resource-format
        valueCode: text/x-gherkin
  Binary/ips-server-gherkin-script:
    name: IPS Server Gherkin Script
    description: Gherkin feature file with the test scenarios for the IPS Server actor.
    exampleBoolean: false
    extension:
      - url: http://hl7.org/fhir/tools/StructureDefinition/implementationguide-resource-format
        valueCode: text/x-gherkin
  # ===== END RENDERING ONLY

pages:
  index.md:
    title: Home
  testing.md:
    title: Testing
  process.md:
    title: Method
  changes.xml:
  downloads.xml:

menu:
  Home: index.html
  Testing: testing.html
  Method: process.html
  Artifacts: artifacts.html
  Downloads: downloads.html
  Changes: changes.html

parameters:
  # path-test mirrors input/testing into package/tests/: the .feature files travel
  # as Gherkin, and a runner resolves TestPlan.suite.input.file against them.
  path-test: input/testing
  incubator-ig: hl7.fhir.uv.testing
  # RENDERING ONLY — resolves the "ig-loader-<file>" data of the Binaries.
  path-binary: input/testing/gherkin

FSHOnly: false
